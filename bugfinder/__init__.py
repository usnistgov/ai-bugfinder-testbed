""" Bugfinder package containing all dataset classes, data processing and utilities.

To import the package in Python scripts, the path must be updated. Do it by either
prepending the python command with ``PYTHONPATH=/path/to/ai-bugfinder``, or adding
this snippet of code before any other ``import`` statement::

    # Sample code for allowing direct imports of the `bugfinder` package.
    from os.path import dirname, join
    import sys

    sys.path.append("/path/to/ai-bugfinder")
"""
